# backbone-board

Static shell for the Bequall Backbone board (decisions queue, task board, pulse).
Contains no secrets: data requires a per-person access token, validated server-side
by the `board` edge function. Canonical source: `bequall-backbone/board/index.html`.
